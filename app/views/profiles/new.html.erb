
<% if current_user && (current_user.profile.nil? || current_user.profile.invalid? ) %>
<div class="background-new questions text-center profile">

  <h2>Welcome <%=current_user.first_name %>   <%=current_user.last_name %>!</h2>

  <h3>To be able to use <em>"Stick To It!"</em>, you need a profile. </h3>
  <br/>
  <h4>Please fill in the following details to make your profile complete. Some questions you could answer in your bio:</h4>
<h4>
  <ul>
    <li>What is the best place in the world you would like to live in?</li>
    <li>What is the one career you would love to enter?</li>
    <li>Which movie influenced your life most?</li>
  </ul>
</h4>


<section class="profile-form">
  <div class="container">
    <div class="row">
      <div class="col-sm-12 col-md-4 col-md-offset-4">
        <div class="form-container">
         <!-- <div class= "logincontainer" > -->
         <%= simple_form_for [@profile] do |f| %>
          <%= f.error_notification %>
         <h5><%= f.input :bio %></h5>
         <h5><%= f.input :photo, as: :attachinary %></h5>
         <h5><%= f.input :photo_cache, as: :hidden %></h5>
         <%= f.submit "write something funny", class:"navbar-wagon-item navbar-wagon-link button-log" %>
         <% end %>
       </div>
     </div>
   </div>
 </div>
</section>

</div>

<% elsif current_user %>
<div class="background-new questions text-center profile">



You already have a profile, <%= current_user.first_name %> <%= current_user.last_name %>.
 <%= link_to "Go to your profile", profile_path(current_user.profile)  %>

 </div>

<% else %>

You cannot access this page

<% end %>





