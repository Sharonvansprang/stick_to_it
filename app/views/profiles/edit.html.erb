<div class="banner-background1 profile-edit-container text-center background-edit">
<% if current_user && @user == current_user%>

<h1> <%= current_user.first_name %>
    <%= current_user.last_name %></h1>
    <div class="profile-edit-form">

      <%= simple_form_for [@profile] do |f| %>
      <h3><%= f.input :bio, :label => "Edit your profile" %></h3>
      <%= f.input :photo, as: :attachinary %>
      <%= f.input :photo_cache, as: :hidden %>
      <h5> <%= f.submit "update", class: "button-navbar"%></h5>
      <% end %>
    </div>


<% elsif current_user %>

Whoops, did you get lost?


<% else %>

You cannot access this page


<% end %>


  </div>

